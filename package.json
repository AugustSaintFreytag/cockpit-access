{
	"name": "cockpit-access",
	"version": "2.0.0",
	"description": "A thin data access layer, models and interfaces for Cockpit CMS.",
	"main": "dist/index.js",
	"typings": "types/index.d.ts",
	"repository": "git@gitlab.com:apricum/cockpit-access.git",
	"author": "August Saint Freytag <night@apricummedia.com>",
	"license": "MIT",
	"private": false,
	"dependencies": {
		"axios": "^0.21.1"
	},
	"scripts": {
		"build": "rm -r dist types; tsc && tscpaths -p ./tsconfig.json -s ./src -o ./dist",
		"prepare": "rm -r dist types; tsc && tscpaths -p ./tsconfig.json -s ./src -o ./dist"
	"devDependencies": {
		"@types/node": "^14.14.22",
		"tscpaths": "^0.0.9",
		"typescript": "^4.1.0"
	}
}
