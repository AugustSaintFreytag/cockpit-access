{
	"name": "cockpit-access",
	"version": "2.0.0",
	"description": "A thin data access layer, models and interfaces for Cockpit CMS.",
	"repository": "git@gitlab.com:apricum/cockpit-access.git",
	"author": "August Saint Freytag <night@apricummedia.com>",
	"license": "MIT",
	"private": true,
	"source": "src/index.ts",
	"main": "dist/index.js",
	"types": "dist/types/index.d.ts",
	"scripts": {
		"build": "yarn; yarn run build-clean; yarn run build-sources; yarn run build-declarations",
		"build-clean": "rm -r dist/*",
		"build-sources": "parcel build src/index.ts",
		"build-declarations": "tsc --emitDeclarationOnly",
		"prepare": "yarn run build"
	},
	"dependencies": {
		"axios": "^0.21.1"
	},
	"devDependencies": {
		"@types/node": "^14.14.22",
		"parcel-bundler": "^1.12.4",
		"typescript": "^4.1.0"
	}
}
